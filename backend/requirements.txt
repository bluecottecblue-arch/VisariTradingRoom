# StrategyForge — Backend dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.0
pydantic-settings==2.2.1
python-multipart==0.0.9

# Database (optional — app works without it using in-memory store)
sqlalchemy==2.0.30
asyncpg==0.27.0
psycopg[binary]>=3.1,<4
aiosqlite==0.20.0
greenlet==3.0.3
psycopg2-binary==2.9.9

# Task queue (optional — backtest runs synchronously without it)
celery==5.4.0
redis==5.0.4

# LLM — required for parse and bot generation
anthropic==0.28.0

# Quant / backtest — required
pandas==2.2.2
numpy==1.26.4

# HTTP client — required for Polygon.io data (optional if using demo data only)
httpx==0.27.0

# Utils
python-dotenv==1.0.1
openai>=1.12.0
google-generativeai>=0.4.0

# Pagamenti / abbonamenti
stripe>=9.0.0
email-validator>=2.1.0

# Mean Reversion Lab — test statistici e analisi quantitativa
statsmodels>=0.14.0
scipy>=1.12.0
matplotlib>=3.8.0
openpyxl>=3.1.0
pyarrow>=15.0.0
yfinance>=0.2.40
